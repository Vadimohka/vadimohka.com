# Task result — T08

## Status

`complete`

## Summary

Added a development-only Playwright + axe-core browser suite and an optional manual GitHub Actions workflow. The suite serves the static site locally and covers all required viewports, overflow, disclosure-menu behavior, skip-link focus, contact intents, JavaScript-disabled navigation, reduced motion, deterministic screenshots, and serious/critical axe findings.

## Files changed

- `qa/browser/check.mjs`
- `qa/browser/package.json`
- `qa/browser/package-lock.json`
- `.github/workflows/browser-qa.yml`
- `.gitignore`
- `README.md`

## Claims changed

No public claims changed.

## Implementation notes

- Browser dependencies live under `qa/browser` and are excluded from the deployment artifact.
- The manual workflow installs Chromium, runs the same suite and uploads `T08-*.png` screenshots.
- The suite intentionally fails on serious/critical axe violations while leaving minor/informational findings available for review.
- Screenshot output is ignored locally to keep binary QA artefacts out of production commits.

## Acceptance criteria

| Criterion | Status | Evidence |
|---|---|---|
| Browser tests cover all listed viewports | pass | Playwright suite covers 1440×1000, 1024×768, 768×1024, 390×844 and 320×568. |
| Mobile navigation and skip link automated | pass | Click/open, Escape/focus return, first-Tab skip link and main focus assertions pass. |
| Serious/critical axe findings fail CI | pass | `AxeBuilder` runs on index/contact/proof and throws on serious/critical violations; clean run passes. |
| Screenshots reviewable/stable | pass | Full-page viewport screenshots written as `qa/screenshots/T08-home-*.png`; workflow uploads them. |
| Production output has no browser package | pass | Browser package is under `qa/`, excluded by deploy rsync; artifact inspection confirms absence. |
| Baseline static QA passes | pass | `node qa/check.mjs` passes after browser suite. |

## Tests and checks

```text
cd qa/browser && npm ci && npx playwright install chromium && npm test
PASS — browser QA covered 5 viewports, menu/skip/no-JS/reduced-motion/contact routes and axe

Intentional regression: replaced focus return with body focus
EXPECTED FAIL — 768: focus did not return to menu button

Restored implementation and reran npm test
PASS

node qa/check.mjs
PASS — 12 pages, all static checks green

git diff --check
PASS
```

## Responsive / accessibility review

- Viewports: 1440×1000, 1024×768, 768×1024, 390×844 and 320×568.
- Browsers: Playwright Chromium; Firefox/WebKit are documented as future matrix additions when browser binaries are available.
- Keyboard: menu click/Enter/Space/Escape, focus return, skip link and no focus trap.
- Automated scan: axe-core WCAG 2A/AA tags; no serious/critical violations.
- Screenshots: `qa/screenshots/T08-home-1440.png`, `T08-home-1024.png`, `T08-home-768.png`, `T08-home-390.png`, `T08-home-320.png` (ignored QA artefacts).

## Owner input still required

- None.

## Known limitations

- This environment ran Chromium; the workflow is intentionally manual so Firefox/WebKit and physical-device checks can be added without slowing every deploy.

## Rollback

Revert `qa/browser/`, `.github/workflows/browser-qa.yml`, README/.gitignore additions and this result file.

## Next eligible task

`T09`
