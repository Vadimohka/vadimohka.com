# Task result — T07

## Status

`complete`

## Summary

Extended the pure-Node QA gate into a deterministic brand/SEO/release guard. It now validates claim/source ledger integrity, production TODO/placeholder safety, JSON-LD parsing and stable identity, metadata equality, navigation parity, public-signal records, canonical/sitemap exactness, robots references, contact form actions, and deployment-sensitive sitemap paths. Added an offline regression fixture test for the new failure modes.

## Files changed

- `qa/check.mjs`
- `qa/test-regressions.mjs`
- `qa/fixtures/bad-placeholder.html`
- `README.md`

## Claims changed

No public claims changed. The QA gate now enforces the existing C###/S### control layer.

## Implementation notes

- Checks remain offline and dependency-free; no network call is required for CI.
- The fixture is under `qa/` and is excluded from production by the deployment workflow.
- Public-signal validation targets the structured records on `expert.html` and requires date, role/format and a direct external source.
- The checker emits page/file-specific actionable failures rather than broad natural-language copy assertions.

## Acceptance criteria

| Criterion | Status | Evidence |
|---|---|---|
| Each regression has a clear actionable error | pass | Guards name the page/file and missing invariant (TODO, source ID, JSON-LD, metadata, sitemap, form action, signal record). |
| Deterministic offline checker | pass | `node qa/check.mjs` completes without network in ~0.06s. |
| Clean site passes in CI | pass | `PASS — 12 pages, all static checks green`. |
| Known bad fixture fails relevant guards | pass | `node qa/test-regressions.mjs` exercises TODO and placeholder form-action detection. |
| Runtime remains reasonable | pass | Pure Node run <0.1s locally. |
| README documents checks | pass | README lists static, ledger, schema, navigation, signal and sitemap checks plus regression command. |

## Tests and checks

```text
node qa/check.mjs
PASS — 12 pages, all static checks green

node qa/test-regressions.mjs
PASS — bad placeholder fixture exercises TODO and form-action guards

git diff --check
PASS
```

## Responsive / accessibility review

- Viewports: no layout changes; T02/T03 responsive checks remain authoritative.
- Browsers: not required for this offline QA task.
- Keyboard: unchanged from prior tasks.
- Automated scan: expanded dependency-free QA passes.
- Screenshots: no visual changes.

## Owner input still required

- None.

## Known limitations

- QA does not replace browser/axe/Lighthouse checks; those are covered by later technical tasks.

## Rollback

Revert `qa/check.mjs`, `qa/test-regressions.mjs`, `qa/fixtures/bad-placeholder.html`, README additions and this result file.

## Next eligible task

`T08`
