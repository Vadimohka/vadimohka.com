# Task result — T02

## Status

`complete`

## Summary

Replaced the narrow-screen horizontal navigation with a semantic disclosure menu while preserving the static link list for no-JavaScript use. Added a skip-to-main-content link, predictable focus return on Escape, link-close behavior, responsive overflow protection, and reduced-motion-safe skip-link scrolling.

## Files changed

- `*.html` — skip link, focusable main target, menu button and controlled primary navigation.
- `assets/site.css` — skip-link treatment, disclosure states, mobile layout and progressive-enhancement rules.
- `assets/site.js` — menu state, keyboard/outside/navigation close behavior, focus management and skip-link enhancement.
- `qa/check.mjs` — menu/skip-link/main-target assertions and updated navigation parser.

## Claims changed

No brand, evidence or public claims changed.

## Implementation notes

- The menu is hidden only after JavaScript adds the `js` class and initializes the mobile state; without JavaScript the links remain in the document and visible/scrollable.
- The native button is supplemented with explicit Enter/Space handling for consistent browser-bridge behavior.
- Desktop navigation remains the existing inline flex layout; only viewports at or below 768px use the disclosure presentation.
- Hidden links use the `hidden` attribute, so they are removed from the tab order while closed.

## Acceptance criteria

| Criterion | Status | Evidence |
|---|---|---|
| Every nav item visible after one action at 320/390px | pass | In-app browser at 320 and 390px: menu opens, all nine links are rendered in one vertical list. |
| Fully keyboard operable | pass | Browser locator test: Enter and Space open the menu; native button has an accessible name. |
| Escape closes and restores focus | pass | Browser locator test: Escape sets `aria-expanded=false`, `hidden=true`, and active element returns to `.menu-toggle`. |
| Hidden links are not tabbable | pass | Closed state uses `hidden=true`; browser state confirmed `display:none`. |
| Skip link is near-first and targets main | pass | Every production page has the first header-adjacent skip link and `<main id="main-content" tabindex="-1">`; static QA enforces both. |
| No general horizontal overflow | pass | Browser viewport checks at 320, 390, 768 and 1440px report `scrollWidth === innerWidth`. |
| `node qa/check.mjs` passes | pass | `PASS — 12 pages, all static checks green`. |

## Tests and checks

```text
node qa/check.mjs
PASS — 12 pages, all static checks green

git diff --check
PASS

Responsive browser state checks — 320, 390, 768, 1440px
PASS — no overflow; mobile button visible and links hidden when closed; desktop unchanged

Keyboard browser checks — click, Enter, Space, Escape
PASS — open state, focus to first link, close state and focus return verified

Progressive-enhancement source check
PASS — no `hidden` attribute in HTML; hide rules are scoped to `html.js`; no-JS horizontal fallback remains available
```

## Responsive / accessibility review

- Viewports: 320×568, 390×568, 768×568 and 1440×900 checked in the in-app Chromium browser.
- Browsers: Chromium/in-app browser; no external browser service was required.
- Keyboard: Skip link, menu Enter/Space, Escape return-focus, navigation close and no focus trap reviewed.
- Automated scan: static QA menu, skip-link, main-target and navigation parity checks pass.
- Screenshots: visual viewport snapshots reviewed at all four required widths; the in-app browser bridge exposes visual inspection but does not export local PNG paths.

## Owner input still required

- None.

## Known limitations

- Physical iOS Safari/Android Chrome and Firefox/WebKit engines remain follow-up validation outside this static task.

## Rollback

Revert the T02 changes in `*.html`, `assets/site.css`, `assets/site.js`, `qa/check.mjs`, and this result file; no unrelated task files are required.

## Next eligible task

`T03`
